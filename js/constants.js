var DRAW_INTERVAL_MS = 1000 / 60;

var KEY_UP = 38;
var KEY_RIGHT = 39;
var KEY_DOWN = 40;
var KEY_LEFT = 37;
var KEY_SPACE = 32;

var MIN = 10;
var MAX = 500;

function getRandom() {
    return Math.floor(Math.random() * (MAX - MIN) + MIN);
}

